<%= turbo_frame_tag(dom_id(product)) do %>
  <% if defined?(updated_product) && updated_product == product %>
  <li class="flex mb-6 product-highlight">
  <% else %>
  <li class="flex mb-6">
  <% end %>

    <%= button_to line_items_path(product_id: product), class: "w-40 h-48 mr-6 shadow cursor-pointer" do %>
      <%= image_tag product.image, class: 'w-full h-full object-contain' %>
    <% end %>
    
    <div>
      <h2 class="font-bold text-lg mb-3"><%= product.title %></h2>
      
      <p>
        <%= sanitize product.description %>
      </p>

      <div class="mt-3">
        <%= number_to_currency product.price %>

        <%=
          button_to t(".add_html"),
          line_items_path(product_id: product),
          form_class: "inline",
          class: "ml-4 bg-green-600 text-white rounded-lg py-1 px-2 cursor-pointer"
        %>
      </div>
    </div>
  </li>
<% end %>